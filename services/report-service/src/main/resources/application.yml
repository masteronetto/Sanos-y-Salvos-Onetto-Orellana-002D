spring:
  application:
    name: report-service
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/sanosysalvos}
    username: ${DB_USERNAME:sanos}
    password: ${DB_PASSWORD:sanos123}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8082
